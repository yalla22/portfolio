// Convenience re-export so integrators can import the showcase section under
// either name. The canonical implementation lives in VisualShowcase.jsx.
export { default } from "./VisualShowcase.jsx";
